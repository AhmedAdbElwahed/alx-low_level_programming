#include "main.h"

/**
 * add - adds two numbers and retruns the result.
 * @x: the first number.
 * @y: the second number.
 * Return: the sum of the tow numbers.
 */

int add(int x, int y)
{
	return (x + y);
}

