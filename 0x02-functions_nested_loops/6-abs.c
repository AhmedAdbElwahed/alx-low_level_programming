#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: integer number
 * Return: the absolute of a given number
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	return (n * -1);
}
